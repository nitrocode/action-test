name: chatops
on:
  issue_comment:
    types: [created]
  workflow_dispatch:

jobs:
  tests-bats:
    runs-on: ubuntu-latest
    steps:
      - name: "Handle common commands and run tests"
        uses: cloudposse/github-action-terraform-ci@35425d96d3eee493574627f86af6ec8b2a50a141
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
