name: chatops
on:
  issue_comment:
    types: [created]
  workflow_dispatch:

jobs:
  tests-bats:
    runs-on: ubuntu-latest
    steps:
      - name: "Handle common commands and run tests"
        uses: cloudposse/github-action-terraform-ci@292e41f84f7fae91adb38160154389fee0d8baaf
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
