name: chatops
on:
  issue_comment:
    types: [created]
  workflow_dispatch:

jobs:
  tests-bats:
    runs-on: ubuntu-latest
    steps:
      - name: "Handle common commands and run tests"
        uses: cloudposse/github-action-terraform-ci@30273b1452819dace1f5e8f49bae4e1be8b39960
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
